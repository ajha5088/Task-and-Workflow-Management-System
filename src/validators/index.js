const userValidators = require('./userValidator');
const taskValidators = require('./taskValidator');
const subTaskValidators = require('./subTaskValidator');

module.exports = {
  userValidators,
  taskValidators,
  subTaskValidators,
};
